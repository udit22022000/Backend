CURSOR...
//find() returns the cursor...// shell displays us 20 documents..
use movieData;
db.movies.find().count()

const cursor = db.movies.find()
cursor.next()  //will throw the very first document..
cursor.next()  //will throw the second doc...basically the next one...
cursor.hasNext()  //to check wheather there is data to be fetched or not...

cursor.forEach(doc => {printjson(doc)})  //iterating with the help of cursor...

